# Optional: fallback loan duration in days when neither the YCL library
# config (in __config_PROD cookie) nor an explicit expires_at is available.
# Most libraries use 14 or 21 days.
YCL_BORROW_DAYS=14
